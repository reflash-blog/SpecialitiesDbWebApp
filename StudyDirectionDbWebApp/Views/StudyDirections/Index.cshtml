@model IEnumerable<StudyDirectionDbWebApp.Models.StudyDirection>

@{
    ViewBag.Title = "Список";
}

<p>
    @Html.ActionLink("Добавить", "Create", null, new { @class = "btn btn-primary" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayName("Название")
        </th>
        <th>
            @Html.DisplayName("Специальность")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StudyDirectionName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Speciality.SpecialityName)
        </td>
        <td>
            @Html.ActionLink("Изменить", "Edit", new { id = item.DirectionId }, new { @class = "btn btn-default" })
            @Html.ActionLink("Подробнее", "Details", new { id = item.DirectionId }, new { @class = "btn btn-default" })
            @Html.ActionLink("Удалить", "Delete", new { id = item.DirectionId }, new { @class = "btn btn-default" })
        </td>
    </tr>
}

</table>
