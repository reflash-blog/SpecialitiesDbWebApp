@model StudyDirectionDbWebApp.Models.StudyForm

@{
    ViewBag.Title = "Удаление";
}

<h3>Вы уверены, что хотите удалить форму обучения?</h3>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayName("Форма")
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Form)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Удалить" class="btn btn-default" />
            @Html.ActionLink("Назад", "Index", null, new { @class = "btn btn-primary" })
        </div>
    }
</div>
